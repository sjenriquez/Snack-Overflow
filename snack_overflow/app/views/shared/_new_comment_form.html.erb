<%= form_for :comment, url: {controller: 'comments', action: "create"} do |f| %>
  <%= f.hidden_field :nom_id, :value => nom_id %>

  <%= f.label 'Add a Comment' %>
  <br>

  <%= f.text_area 'body', size: "50x10"  %>
  <br>
  <%= f.submit "Submit" %>
<% end %>
